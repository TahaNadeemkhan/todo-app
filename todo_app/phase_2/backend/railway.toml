[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn src.todo_app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/docs"
restartPolicyType = "on_failure"
