# Neon PostgreSQL Connection
# Get this from: https://neon.tech -> Your Project -> Connection Details
DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# Shared secret for JWT verification (must match Better Auth config)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-shared-secret-key-min-32-chars

# CORS Origins (comma-separated for production)
CORS_ORIGINS=http://localhost:3000

# ============================================
# EMAIL NOTIFICATIONS (Gmail SMTP)
# ============================================
# Your Gmail address (sender email)
EMAIL_ADDRESS=your-email@gmail.com

# Gmail App Password (NOT your regular password!)
# How to generate:
# 1. Go to Google Account > Security
# 2. Enable 2-Factor Authentication (if not already)
# 3. Go to "App Passwords" (search in Google Account settings)
# 4. Select "Mail" and "Windows Computer" (or Other)
# 5. Click Generate - copy the 16-character password
EMAIL_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# SMTP Settings (don't change for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
