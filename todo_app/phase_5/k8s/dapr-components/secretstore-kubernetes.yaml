# Dapr Secret Store Component for Kubernetes Secrets
# Based on Hackathon II Document - Phase V Requirements
apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: kubernetes-secrets
  namespace: default
spec:
  type: secretstores.kubernetes
  version: v1
  metadata:
    # No additional metadata required for Kubernetes secrets
scopes:
  - backend-service
  - notification-service
  - recurring-task-service
